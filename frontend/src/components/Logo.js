import React from 'react'
import styled from '@emotion/styled'

const LogoText = styled.div`
    font-weight: bold;
    font-size: 16px;
    font-family: 'Mali';
`

const  LogoComp = () => {
    return (
        <LogoText>
            PetPing | เว็บไซต์รับอุปการสัตว์
        </LogoText>
    )
}

export default  LogoComp
