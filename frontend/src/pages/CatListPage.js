import AnimalList from '../components/AnimalList';

const DogListPage = () => {
    return (
        <div>
            <AnimalList type="แมว" noWidth />
        </div>
    );
};

export default DogListPage;
